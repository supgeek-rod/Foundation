将已确定的完整技术方案（Vue 3 + Vite + TS + Pinia + Tailwind v4 + shadcn-vue + lucide + vueuse + vuedraggable + idb-keyval + PWA；Open-Meteo 天气；分层架构与目录结构；10 个核心模块设计；7 步实施步骤；验收清单与讨论记录）写入 `docs/PLAN.md` 保存。

仅创建这一个文档文件，不初始化项目、不改任何其他内容。之后你审阅方案，确认后再开始实施。